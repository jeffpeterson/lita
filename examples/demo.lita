fn fib(n)
	if n < 2: return n
	return fib(n - 2) + fib(n - 1)

print "Lita Demo:"
pp 4567.string

let x = 42
pp 3(x)

pp hash("abcd")
pp read("Makefile")
pp write("tmp/demo.txt", "It runs!")

class Point(x, y)
	+ (b) = Point(x + b.x, y + b.y)

let p1 = Point(4, 5)
let p2 = Point(6, 7)

pp Point

pp(p1.+(p2))
pp(p1 + p2)
