#include <stdlib.h>

#include "graph.h"

void init_graph(Graph *graph) {
  // DB *db;
  // int err = db_create(&db, NULL, 0);

  // if (err) {
  //   printf("db_create error: %i", err);
  //   exit(1);
  // }

  //   db->open(db, NULL, )
}

void free_graph(Graph *graph) {}
